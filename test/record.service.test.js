const { MOCK_DATA } = require('../src/config/test.config');
const serviceRecord = require('../src/services/record.service');

describe('Filter function', () => {
  test('it should filter data by criterias', () => {});
});
